# EVM
Language virtual machine

## Build

```shell
mkdir build && cd build
cmake .. -GNinja
ninja evm
```

## Run unit tests

```shell
mkdir build && cd build
cmake .. -GNinja
ninja run_unit_tests
```
## Requirements
  - libgtest-dev package for Ubuntu
